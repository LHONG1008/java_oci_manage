# 已实现功能

[English](./en/function.md)

---

## Telegram 机器人 — Oracle Cloud (OCI)

- [x] 开机（AMD / ARM / Intel，自定义配置）
- [x] IP 管理（查询、更换、自动 DNS 更新）
- [x] 被暂停开机（无需删盘重建）
- [x] IPv6 管理（附加、更换）
- [x] 升降配（CPU、内存）
- [x] 实例管理（终止、改名、强制重启、重置系统、监控开关）
- [x] 磁盘管理（扩容、性能调优、分离/附加、删除）
- [x] 云账户管理（添加管理员、重置密码、查邮箱、删用户）
- [x] 实例状态监控 + 告警通知
- [x] 实例状态监控 + 自动重启
- [x] 一键开放安全组端口
- [x] 甲骨文工作流错误查询
- [x] 多 Profile / 多客户端管理
- [x] 一键测活（批量检测所有账户状态）
- [x] 自动换 IP 监控（带 DNS 绑定、IP 段过滤）
- [x] 快捷开机（保存配置、批量多 Profile 开机）
- [x] 开机提醒标注账号类型（升级号 / 普通号，按订阅 paymentModel 判定）
- [x] 甲骨文订阅信息查询
- [x] 近 3 月流量查询
- [x] 配额查询（实例、网络、存储）
- [x] 运行中任务查看
- [x] 客户端负载查看
- [x] 智能内存占用（补足至 25%）
- [x] 近 3 月花费查询
- [x] 清除所有 2FA 设备
- [x] 一键禁用被封账户
- [x] 删除 API 密钥
- [x] 每日花费和流量报告
- [x] 批量查询邮箱
- [x] Cloudflare 域名快捷操作
- [x] 无需公网 IP 的本地模式

## Telegram 机器人 — AWS

- [x] EC2 实例管理（列表、启动、停止、重启、终止）
- [x] 自定义创建 EC2 实例（AMI、机型、密钥选择）
- [x] Lightsail 轻量实例管理（列表、启动、停止、重启、删除、流量查询）
- [x] 换 IP
- [x] 费用查询
- [x] 配额用量查询

## Telegram 机器人 — GCP

- [x] Compute Engine 实例管理（列表、启动、停止、重启、删除）
- [x] 自定义创建实例（可用区、机型、镜像、磁盘大小、SSH 密钥）
- [x] 换 IP
- [x] 概览统计（实例数量、可用区分布、免费层计数）
- [x] 近 3 月流量查询

## Telegram 机器人 — Azure

- [x] 自定义开机
- [x] 换 IP
- [x] 删除实例
- [x] 查询配额用量
- [x] 删除所有资源

## Telegram 机器人 — DigitalOcean

- [x] Droplet 管理（列表、详情、开机、关机、重启、销毁）
- [x] 多 Profile 切换
- [x] 单实例当月流量查询
 
## Telegram 机器人 — VirtFusion

- [x] 厂商维度实例列表
- [x] 实例详情（状态、CPU、内存、磁盘、IPv4/IPv6、流量、创建时间）
- [x] 开机 / 关机 / 重启 / 强制关机
- [x] 重置系统密码

## Telegram 机器人 — SolusVM

- [x] SolusVM 面板 VPS 管理

---

## Web SSH 终端

- [x] 浏览器内 SSH 连接（密码 / 私钥认证，支持 SOCKS5 代理）
- [x] 多标签页并行终端
- [x] SFTP 文件管理（浏览、上传、下载、删除文件/文件夹、创建目录）
- [x] SFTP 在线文本编辑器（语法高亮，直接编辑服务器文件）
- [x] SFTP 传输管理器面板（并发多任务进度、速度、取消，保留最近完成历史）
- [x] 无 SFTP 子系统主机的 exec 通道文件传输降级（OpenWrt/dropbear/busybox 仍可传文件）
- [x] SSH 端口转发（本地 / 远程）
- [x] SSH 断线自动重连（指数退避策略，支持 reboot / 网络中断场景自动恢复）
- [x] 批量命令（向多个主机同时发送，结果工作台支持连续执行）
- [x] 终端工具条（收藏、搜索、常用工具快捷入口）
- [x] 终端 WebGL 渲染（xterm.js 6.0，环境不支持自动回退 DOM）+ 官方搜索插件（高亮 / 概览标尺 / 计数）
- [x] 多行右键粘贴保护（整段停在输入行、回车再执行，防误粘脚本直接运行）
- [x] 主机标签与分组筛选（卡片内联打标签、交集 AND 过滤、命中计数、搜索兼匹配标签）
- [x] 终端分屏（横向拆分两个独立面板、一键克隆当前会话、刷新自动还原）
- [x] 会话挂起（screen 式显式挂起 + 列表接回，单用户最多 20 个）
- [x] 持久 Shell（关闭浏览器后 shell 进程仍在服务端运行，下次自动接回）
- [x] 资源告警（CPU / 内存 / 磁盘阈值告警，Telegram 推送）
- [x] 多云体检面板（一屏汇总所有云平台实例状态与健康度）
- [x] 会话配置保存与管理
- [x] 集中式 SSH 密钥管理（加密存储、并发智能匹配）
- [x] 主机指纹验证（SHA256）
- [x] 自动检测主机规格（OS、CPU、内存、磁盘）
- [x] 资源监控面板（顶栏显示 CPU / 内存 / 硬盘 / 网络实时指标）
- [x] 对象存储管理（OCI Object Storage Bucket 浏览、文件上传/下载/删除）
- [x] ACME 自动 SSL 证书（Let's Encrypt）
- [x] 云主机同步（一键从 OCI/AWS/GCP/Azure/DO/SolusVM/VirtFusion 发现主机并导入会话列表，SSE 实时进度）
- [x] 云平台配置在线上传、编辑与管理（OCI/AWS/GCP/Azure/DO/SolusVM/VirtFusion，合并模式 + 在线编辑 / 删除单个 Profile + 密钥掩码 + 保存即热加载）
- [x] Web 端客户端升级（版本检查、升级、强制升级、重启服务，触发冷却防重复拉起）
- [x] Web 端客户端运行日志（行数选择、关键字过滤、自动刷新、一键复制）
- [x] Telegram 验证码登录 + 防暴力破解
- [x] 中英文界面切换
- [x] 页面状态记忆（刷新后自动恢复到刷新前的位置，云管理子页面状态同步）
- [x] 在线客服 IM（内置聊天窗口，支持图片消息）
- [x] 响应式布局（移动端友好）
- [x] HTTPS (TLSv1.3) + HTTP/2

---

## Web 云管理面板

### Oracle Cloud

- [x] 实例管理（创建、快捷开机、强开 ARM、启动、关机、重启、终止、重装系统、升降配、改名、修复）
- [x] 创建实例快速配置（AMD 微型 1C/1G、ARM A1 2C/12G 一键预填规格、镜像、公钥、延迟）
- [x] 从现有启动盘创建实例（限单台，自动约束到盘所在可用域）
- [x] 实例列表启动盘信息合并展示
- [x] 强开 ARM（试用期提升 ARM 创建成功率，支持 Web + Telegram）
- [x] 网络管理（换 IP、附加 IPv4/IPv6、保留 IP、删除 IP）
- [x] 卷管理（调整容量、VPU 性能、分离、附加、删除、批量 VPU，卷状态与附加状态徽章全量展示）
- [x] 启动盘重新附加（把已分离的启动盘挂回已停止的实例，无附加盘时自动定位到未附加启动盘面板）
- [x] 创建实例结果轮询（Web 端提交后查询任务状态并展示最终结果）
- [x] A1 配置体检 / 降配（并行扫描各账号 A1.Flex 用量 vs 永久免费额度，账号级 / 批量 / 逐台抢占式降配，仅降不升、绝不自动删实例）
- [x] 用户管理（创建、删除、重置密码、修改邮箱、清除 MFA、改租户名、查看身份域密码策略）
- [x] 统计概览（成本、流量、订阅信息、配额）
- [x] Profile 管理（列表、切换、删除）
- [x] Profile 一键复制为新地区（OCI / AWS，凭据服务端整段复制，仅替换区域）
- [x] API 出站代理按 Profile 配置（Web 面板 + Telegram，概览页标注已配代理的账号）
- [x] 对象存储管理（Bucket 浏览、文件上传/下载/删除）
- [x] 实例监控通知 / 自启 / 每日报告 / 一键测活
- [x] 串行控制台（OCI 实例串行控制台连接，WebSocket 实时终端，Netboot.xyz 救援引导自动化）
- [x] Email Delivery（一键搭建邮件域、DKIM/DNS 自动配置、DKIM 修复、添加发件人、SMTP 凭证管理、测试发信）

### AWS

- [x] EC2 实例管理（列表、启动、停止、重启、终止）
- [x] 创建 EC2 实例（AMI 选择、机型选择、密钥管理、异步创建）
- [x] Lightsail 轻量实例管理（列表、启动、停止、重启、删除、本月监控流量）
- [x] 创建 Lightsail 实例（区域、可用区、系统镜像、套餐、密钥对、名称、数量，套餐按镜像平台过滤，支持导入本地公钥）
- [x] EC2 防火墙 / 安全组管理（挂载、卸载、新建、删除安全组，入站出站规则增删，常用规则一键填充）
- [x] Lightsail 网络/IP 管理（静态 IP 创建/绑定/解绑/释放、换静态 IP、重启换动态 IP、防火墙端口）
- [x] 网络管理（VPC、安全组）
- [x] 费用统计（Cost Explorer 集成）
- [x] 配额用量查询

### GCP

- [x] Compute Engine 实例管理（列表、创建、启动、停止、重启、删除、换 IP）
- [x] 概览统计（实例计数、可用区分布、免费层统计）
- [x] 流量查询（近 3 月收发流量明细）

### Cloudflare DNS

- [x] 域名列表、DNS 记录增删改查
- [x] API Token 认证（最小权限，与 Global API Key 并存，Token 优先）

### DigitalOcean

- [x] Droplet 管理（列表、创建、开机、关机、重启）
- [x] 保留 IP 管理（分配、绑定、解绑、释放）
- [x] 带宽监控（每月流量用量及配额）
- [x] 账单概览

### Azure

- [x] VM 管理（列表、创建、删除、重启、换 IP）
- [x] 资源用量查询

### SolusVM

- [x] VPS 管理（节点列表、仪表盘、开关机、重启）

### VirtFusion

- [x] 实例卡片管理（按厂商分组、状态/规格/IP/创建时间展示）
- [x] 流量进度条与当前周期用量展示
- [x] SSH 一键连接
- [x] 开机 / 关机 / 重启 / 强制关机
- [x] 实例改名
- [x] 系统密码重置

### 云监控与域名监控

- [x] 流量守护（按账号设当月流量阈值，超阈仅通知或自动关停该账号全部区域实例）
- [x] 停机保活（实例意外停机通知 / 自动开机，被流量守护关停的账号当月不自动开）
- [x] 域名与 SSL 证书到期监控（每日定时检查，30/14/7/1 天及已过期分档 Telegram 提醒）
- [x] 从 Cloudflare 一键导入托管域名，中文域名搜索自动转 punycode

### 通用

- [x] 云实例一键 SSH — 所有云平台（含 DO）实例卡片支持直接快速连接 SSH 终端

---

## 云主机同步

- [x] 多云平台主机发现（OCI / AWS EC2 / AWS Lightsail / GCP / Azure / DO / SolusVM / VirtFusion 并行查询）
- [x] 自动导入到 SSH 会话列表（IP 去重、IPv6 支持）
- [x] SSE 实时进度反馈（按平台显示查询状态）

---

## 正在开发

- [ ] 更多云平台管理操作持续完善中
