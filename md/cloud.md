# Web 云管理面板指南

[English](./en/cloud.md)

R-Bot 客户端内置了完整的 Web 云管理面板，在浏览器中即可管理 Oracle Cloud、AWS、GCP、Azure、DigitalOcean、SolusVM、VirtFusion、Cloudflare DNS 等多云平台资源，操作能力与 Telegram 机器人对齐。

---

## 访问方式

启动客户端后，访问：

```
https://你的IP:9527
```

登录后在主机面板中点击云管理相关入口，或通过视图切换进入云管理工作台。

> 首次使用需先在顶部栏「设置」中配置云平台 Profile（或通过 `client_config` 文件配置）。

---

## 概览面板

登录后的首页展示当前 Profile 的概览信息：

| 卡片 | 内容 |
|------|------|
| **成本** | 近 3 个月花费明细 |
| **流量** | 近 3 个月流量使用量 |
| **订阅** | 订阅类型、支付方式、有效期 |
| **配额** | 实例、网络、存储配额使用情况 |

支持切换不同的 Profile 查看不同账户的信息。

---

## Oracle Cloud 管理

### 实例管理

| 操作 | 说明 |
|------|------|
| 查看实例列表 | 展示所有实例及状态，启动盘信息合并展示 |
| 创建实例 | 选择配置规格、镜像、网络等参数创建新实例 |
| 快速配置 | 一键预填 AMD 微型 1C/1G 或 ARM A1 2C/12G，同时填好镜像、硬盘、数量、延迟并自动选中公钥 |
| 从启动盘创建 | 用现有未附加的启动盘直接开机，保留原有系统和数据；限单台，且只能开在该盘所在可用域 |
| 强开 ARM | 试用期提升 ARM 实例创建成功率（需短暂使用账户收费功能，风险自担） |
| 快捷开机 | 基于已保存的配置快速创建实例 |
| 创建结果轮询 | 提交后页面持续查询任务状态并展示最终结果，不必只等 Telegram 通知；查询超时时结果仍会通过 Telegram 送达 |
| 启动 / 关机 / 重启 | 基础实例操作 |
| 终止实例 | 删除实例（可选保留引导卷） |
| 重装系统 | 重置为初始系统镜像 |
| 升降配 | 调整 CPU 和内存规格 |
| 修改名称 | 修改实例显示名称 |
| 修复实例 | 尝试修复异常实例 |

![创建实例](../screenshots/cloud-create.jpg)

### 网络 / IP 管理

| 操作 | 说明 |
|------|------|
| 查看 VNIC 列表 | 展示网卡、私有 IP、公网 IP |
| 换 IPv4 | 更换公网 IPv4 地址 |
| 换 IPv6 | 更换 IPv6 地址 |
| 删除 IP | 删除公网 IP 或 IPv6 |
| 附加 IPv4 | 给实例添加额外的 IPv4 |
| 附加 IPv6 | 创建新的 IPv6 地址 |
| 附加保留 IP | 附加预留的公网 IP |

### 卷管理

| 操作 | 说明 |
|------|------|
| 查看卷列表 | 展示所有块存储卷，含卷状态和附加状态徽章（已附加 / 已分离 / 附加中 / 分离中），不再按状态预过滤 |
| 调整容量 | 增加卷大小（不可缩小） |
| 调整 VPU | 修改性能单位提升 IO 速度 |
| 批量 VPU | 一键拉满所有卷的 VPU |
| 分离卷 | 将卷从实例分离，实例必须先处于已停止状态 |
| 附加启动盘 | 把已分离的启动盘重新挂回实例，目标实例须为已停止状态 |
| 删除卷 | 永久删除块存储卷 |

分离状态的启动盘集中在「未附加启动盘」面板。实例没有附加启动盘时，点「启动盘」会展开并定位到该面板，而不是提示无信息。

### A1 配置体检 / 降配

针对 OCI ARM（A1.Flex）永久免费额度的专项工作台，帮助把超出免费额度的账号一键降回合规配置。

| 操作 | 说明 |
|------|------|
| 配置体检 | 并行扫描各账号当前区域 A1.Flex 用量，对比免费额度上限（2 核 12G），标记「超开 / 已符合额度 / 查询失败」 |
| 账号降配 | 弹窗自定义目标配置（默认推荐 2 核 12G），在账号内多台实例间均分 |
| 批量降配 | 一键全选可降配账号，批量提交 |
| 逐台降配 | 单台实例填入推荐值后降配 |
| 降配方式 | 复用抢机式 resize（撞容量自动重试直到成功 + TG 通知 + 任务列表可取消），仅降不升、绝不自动删实例 |
| 删除实例 | 逐台二次确认删除，不批量盲删 |
| 权限范围 | 非闪电用户仅体检当前账号，闪电用户可一次体检 / 降配全部账号 |

### 用户管理

| 操作 | 说明 |
|------|------|
| 查看用户列表 | 展示租户下所有用户 |
| 创建用户 | 添加新的管理员用户 |
| 修改邮箱 | 更新用户邮箱地址 |
| 重置密码 | 重置用户登录密码（支持系统生成强密码） |
| 清除 MFA | 一键清除用户绑定的所有多因素认证设备 |
| 修改租户名 | 更改租户显示名称 |
| 删除用户 | 删除指定用户 |
| 密码策略 | 查看身份域密码策略（有效期、复杂度要求等） |

### 对象存储 (OSS)

| 操作 | 说明 |
|------|------|
| 浏览 Bucket | 列出所有存储桶 |
| 浏览文件 | 支持前缀过滤和分页浏览 |
| 上传文件 | 流式上传，防路径遍历 |
| 下载文件 | 流式下载 |
| 删除文件 | 删除单个对象 |
| 创建文件夹 | 创建目录结构 |

### Profile 管理

| 操作 | 说明 |
|------|------|
| 查看 Profile 列表 | 展示所有配置的 OCI Profile，并标注哪些账号已配 API 出站代理 |
| 切换 Profile | 切换到不同的云账户操作 |
| 复制为新地区 | 一键把现有 Profile 复制成另一个区域的新 Profile，凭据在服务端整段复制不经前端，只替换区域 |
| API 出站代理 | 按 Profile 单独配置出站代理，让不同账号的 API 请求走各自的出口 |
| 删除 Profile | 删除不需要的 Profile |

### Email Delivery（邮件服务）

| 操作 | 说明 |
|------|------|
| 一键搭建 | 输入域名和发件人地址，自动完成邮件域创建、DKIM 签名、DNS 记录配置、发件人注册、SMTP 凭证生成全流程 |
| DNS 模式 | 支持 Cloudflare 自动配置和手动配置两种模式，手动模式会暂停展示 DNS 记录等待用户确认 |
| 搭建进度 | 异步执行，实时进度条展示当前步骤，离开页面可恢复 |
| 域名管理 | 查看邮件域列表、DKIM 验证状态、域生命周期状态 |
| DKIM 信息 | 查看域的 DKIM CNAME 记录详情，供手动配置 DNS 参考 |
| DKIM 修复 | DKIM 验证失败时，通过 Cloudflare 自动重新配置 DNS CNAME 记录（异步，支持进度查询） |
| 发件人管理 | 查看指定域下的发件人列表和状态 |
| 添加发件人 | 为已有邮件域添加新的发件人地址 |
| SMTP 配置 | 查看 SMTP 连接信息（服务器/端口/用户名） |
| 重新生成凭证 | 重新生成 SMTP 密码（一次性显示，请妥善保存） |
| 测试发信 | 通过 OCI HTTP API 发送测试邮件，支持自定义收件人/主题/内容 |
| 删除域 | 级联删除邮件域及关联的 DKIM、发件人资源 |

### 串行控制台

通过 OCI Console Connection 连接实例的串行控制台，适用于实例无法通过 SSH 访问时的紧急运维。

| 操作 | 说明 |
|------|------|
| 连接控制台 | 自动生成临时 RSA 密钥对，建立 SSH-over-SSH 隧道 |
| WebSocket 终端 | 浏览器内实时串行终端交互 |
| Netboot.xyz 自动化 | 自动检测 UEFI/GRUB 菜单，引导 Netboot.xyz 救援系统 |
| 连接管理 | 自动复用已有连接，30 分钟密钥 TTL，定时清理过期连接 |
| 用户确认 | 危险操作前的半自动确认工作流 |

---

## AWS 管理

### EC2 实例管理

| 操作 | 说明 |
|------|------|
| 查看实例列表 | 展示所有 EC2 实例及实时状态 |
| 创建实例 | 选择 AMI 镜像、实例类型、SSH 密钥，异步创建并跟踪进度 |
| 启动 / 停止 / 重启 | 基础实例电源操作 |
| 终止实例 | 永久删除 EC2 实例 |

### Lightsail 轻量实例管理

| 操作 | 说明 |
|------|------|
| 查看轻量实例列表 | 展示 Lightsail 实例公网 IP、套餐、镜像、规格、区域和创建时间 |
| 创建实例 | 在 AWS 创建页切换到 Lightsail 产品线，选择区域、可用区、系统镜像、套餐、密钥对、名称和数量。套餐按镜像平台和最低算力过滤，本地已存的公钥可直接导入。异步创建，完成后列出实例和公网 IP 并发 Telegram 通知 |
| 启动 / 关机 / 重启 | 基础电源操作 |
| 删除实例 | 删除 Lightsail 实例，危险操作需确认 |
| 本月监控流量 | 查看入站、出站、合计流量和套餐标称额度；该值来自监控指标，不等同于账单用量 |

### Lightsail 网络 / IP 管理

| 操作 | 说明 |
|------|------|
| 静态 IP 管理 | 创建并绑定静态 IP、解绑、释放 |
| 换静态 IP | 创建新静态 IP、切换绑定并释放旧静态 IP |
| 重启换动态 IP | 未绑定静态 IP 时，通过关机再启动尝试更换动态公网 IP |
| 防火墙端口 | 查看并保存公开端口规则，支持单端口和端口段 |

### 网络管理

| 操作 | 说明 |
|------|------|
| VPC 管理 | 查看和管理 VPC 资源 |

### EC2 防火墙 / 安全组

EC2 实例卡片上的「防火墙」入口，在一个弹窗里管完安全组和规则。

| 操作 | 说明 |
|------|------|
| 查看安全组 | 分为已关联安全组和同 VPC 的其他安全组两栏 |
| 挂载 / 卸载 | 给实例挂上或摘掉安全组，实例至少保留一个 |
| 新建并挂载 | 填名称和描述新建安全组并直接挂到实例上 |
| 删除安全组 | 仅在未被任何资源引用时可删 |
| 入站 / 出站规则 | 增删规则，支持 tcp / udp / icmp / icmpv6 / 全部协议、端口范围、IPv4 和 IPv6 CIDR |
| 常用规则 | SSH 22、HTTP 80、HTTPS 443、Ping、放行全部，点一下填进表单，确认后再添加 |
| 引用型规则 | 引用其他安全组或前缀列表的规则以只读方式展示 |

规则为空时面板会直接说明后果：没有入站规则则所有入站连接（含 SSH）被拒绝；没有出站规则则实例无法主动访问外网，连 apt/yum 都走不通。

### 统计与监控

| 操作 | 说明 |
|------|------|
| 费用统计 | AWS Cost Explorer 集成，查看费用明细 |
| 用量监控 | CloudWatch / Lightsail 指标查询 |
| 配额查询 | 查看资源配额使用情况 |

---

## GCP 管理

### Compute Engine 实例管理

| 操作 | 说明 |
|------|------|
| 查看实例列表 | 跨可用区聚合展示所有实例及状态 |
| 创建实例 | 选择可用区、机型、镜像、磁盘大小，支持免费层提示 |
| 启动 / 停止 / 重启 | 基础实例电源操作 |
| 删除实例 | 异步删除，完成后 Telegram 通知 |
| 换 IP | 释放旧外部 IP 并分配新 IP |

### 统计与概览

| 操作 | 说明 |
|------|------|
| 概览 | 实例数量统计、可用区分布、e2-micro 免费层计数 |
| 流量查询 | 近 3 个月收发流量明细 |

---

## Cloudflare DNS 管理

| 操作 | 说明 |
|------|------|
| 查看域名列表 | 列出 Cloudflare 托管的所有域名 |
| 查看 DNS 记录 | 查看域名下的所有解析记录 |
| 添加记录 | 新增 A/AAAA/CNAME 等 DNS 记录 |
| 修改记录 | 编辑现有 DNS 记录 |
| 删除记录 | 删除指定 DNS 记录 |

---

## DigitalOcean 管理

### Droplet 管理

| 操作 | 说明 |
|------|------|
| 查看 Droplet 列表 | 展示所有 Droplet 及状态、IP、规格 |
| 创建 Droplet | 选择镜像、区域、规格（5 种类型）、SSH 密钥，支持批量创建和 cloud-init 脚本 |
| 开机 / 关机 / 重启 | 基础电源操作 |

### 保留 IP 管理

| 操作 | 说明 |
|------|------|
| 查看保留 IP 列表 | 展示所有保留 IP 及绑定状态 |
| 分配保留 IP | 为 Droplet 分配新的保留 IP |
| 绑定 / 解绑 | 将保留 IP 绑定到 Droplet 或从 Droplet 解绑 |
| 释放 | 删除保留 IP |

### 监控与账单

| 操作 | 说明 |
|------|------|
| 带宽监控 | 实例卡片直接显示当前周期流量，按日分段积分计算近似值（DO 未提供官方流量接口） |
| 账单概览 | 账户余额、本月费用 |

---

## Azure 管理

| 操作 | 说明 |
|------|------|
| 查看 VM 列表 | 展示所有虚拟机及状态 |
| 创建 VM | 新建虚拟机 |
| 删除 VM | 删除虚拟机 |
| 重启 VM | 重启虚拟机 |
| 换 IP | 更换公网 IP 地址 |
| 资源用量 | 查看配额使用情况 |

---

## SolusVM 管理

| 操作 | 说明 |
|------|------|
| 查看节点列表 | 列出所有 VPS 节点 |
| 节点仪表盘 | 查看 VPS 状态详情 |
| 开机 / 关机 / 重启 | VPS 电源操作 |

---

## VirtFusion 管理

| 操作 | 说明 |
|------|------|
| 厂商分组展示 | 按配置的 vendor alias 分组展示实例卡片 |
| 查看实例列表 | 展示状态、IPv4/IPv6、CPU、内存、磁盘、创建时间 |
| 流量面板 | 显示当前计费周期流量使用量与百分比 |
| SSH 快捷连接 | 对运行中的实例一键跳转到 Web SSH |
| 开机 / 关机 / 重启 / 强制关机 | 常用电源操作 |
| 修改名称 | 直接在卡片上重命名实例 |
| 重置密码 | 返回新的系统密码（仅展示一次） |
| 配置错误提示 | Token 失效或面板不可访问时，工作台直接提示并支持跳转重新配置 |

---

## 云监控

工作台的「云监控」标签页，两组规则，与机器人端的实例监控是同一份配置，任一端改动另一端同步。

### 流量守护（按账号）

OCI 免费额度为每月 10240 GB，超出按量计费。为账号设一个当月流量阈值，达到后触发。

| 项目 | 说明 |
|------|------|
| 监控账号 | 按 Profile 逐个设置 |
| 流量阈值 | 单位 GB。账单数据有 4~24 小时延迟，建议填 9000 而非贴着上限，留出止损时间 |
| 仅通知我 | 只发 Telegram，不动实例 |
| 自动关机 | 立即关停该账号**全部区域**的实例（SOFTSTOP 优雅停机），触发即执行，无二次确认 |
| 当月用量 | 来自 Oracle 账单接口，非实时值 |

选自动关机需要知道：手动开机后若仍超阈，最迟 1 小时内会被再次关停；关机时间过长，A1 等抢手机型的容量配额可能被 Oracle 回收，重开需要重新抢机。

### 停机保活（全部账号）

| 开关 | 说明 |
|------|------|
| 实例停机通知 | 每 8 分钟检查一次，实例意外停机时发 Telegram |
| 停机自动开机 | 检测到停机自动启动；被流量守护关停的账号当月不会自动开机，避免继续跑流量 |

---

## 域名监控

工作台的「域名监控」标签页。每日定时检查域名注册到期和 SSL 证书到期，临期通过 Telegram 提醒。

| 操作 | 说明 |
|------|------|
| 添加域名 | 手动添加单个域名 |
| 从 Cloudflare 导入 | 批量导入 Cloudflare 托管的域名，同名跳过，超出数量上限时提示未导入的部分 |
| 到期提醒开关 | 逐个域名开关提醒 |
| 立即检查 | 不等定时任务，当场探测一次 |
| 搜索 | 支持中文域名，自动转 punycode 匹配 |
| 删除 | 删除后不再监控 |

提醒档位为 30 / 14 / 7 / 1 天及已过期，按档位去重，同一档只提醒一次。

建议填注册域名（如 `example.com`）。子域名查不到注册信息，域名到期会显示「未知」，证书检查不受影响。单次检查失败时保留上一次的数据并标为「未知」，不覆盖已知值。

权限范围：添加、导入、开启提醒需要闪电权益；停用、删除、查看和「立即检查」不需要。

---

## 设置

### 实例监控

| 功能 | 说明 |
|------|------|
| 实例监控通知 | 定时监控实例状态，异常时 Telegram 通知 |
| 监控自启 | 异常时自动尝试启动实例 |
| 每日报告 | 定时推送花费和流量报告 |
| 一键测活 | 批量检测所有 Profile 账户状态 |
| 开机提醒标注账号类型 | 开机通知标注账号为升级号 / 普通号（按 OCI 订阅 paymentModel 判定，不确定则不标注） |

### 客户端维护

顶栏设置下拉菜单里的「客户端升级」和「客户端日志」，与机器人端的「32. 升级客户端」「34. 查询最新日志」对应，不需要闪电权益。

**客户端升级**

| 操作 | 说明 |
|------|------|
| 版本检查 | 显示当前版本和最新版本，远端不可达时降级为「无法检查」，不阻断页面 |
| 立即升级 | 下载最新版本并重启客户端 |
| 强制升级 | 不比对版本直接重新下载覆盖安装，适用于版本查询本身不通的情况 |
| 重启服务 | 只重启客户端进程，不更新版本 |

升级和重启共用 5 分钟冷却，期间再点会提示等待本次完成 — 这挡的是两端连点拉起多个安装进程互相踩踏。升级过程中 Web 终端和 SSH 会话会断开，通常 1~3 分钟恢复。

**客户端日志**

在浏览器里直接看 `log_r_client.log`，可选 100 / 300 / 1000 行、按关键字过滤、5 秒自动刷新、一键复制。面板会标出总行数、文件大小和更新时间；文件过大时只回溯最近 2MB。

### ACME 证书

在设置页面配置 Let's Encrypt 自动证书，详见 [Web SSH 终端指南 — SSL 证书](./webssh.md#ssl-证书配置)。

### 云平台配置管理

在 Web 界面中直接上传、编辑和管理云平台 API 配置，覆盖 OCI/AWS/GCP/Azure/DO/SolusVM/VirtFusion 共 7 种云，无需手动编辑 `client_config` 文件。

| 功能 | 说明 |
|------|------|
| OCI 配置上传 | 粘贴 API 配置文本 + 上传 PEM 密钥文件，自动配置 key_file 路径 |
| AWS 配置上传 | 粘贴 Access Key ID / Secret Access Key 配置 |
| GCP 配置上传 | 上传 Service Account JSON 密钥文件，自动提取凭据 |
| DO 配置上传 | 粘贴 DigitalOcean API Token |
| Azure 配置上传 | 粘贴 appId/password/tenant 配置 |
| SolusVM 配置上传 | 粘贴 API 地址和密钥配置 |
| VirtFusion 配置上传 | 粘贴 host/token/preset 或使用预置厂商快速填充 |
| 合并模式 | 同名 Profile 自动跳过并提醒，新 Profile 追加到配置中 |
| Profile 在线编辑 | 已配置的 Profile 以标签列表展示，点开即可就地修改字段，改字段不再需要 SSH 改文件 |
| Profile 复制为新地区 | OCI / AWS 的 Profile 可一键复制成另一区域的新 Profile，只替换区域，AWS 可用区形态自动归一化 |
| API 出站代理 | 在 Profile 编辑面板底部按账号配置出站代理；停用需点「移除代理」，清空输入框保存不算停用 |
| 删除单个 Profile | 删除指定 Profile，并同步清理默认 Profile 的悬挂引用 |
| 密钥掩码 | 密钥 / Token 等敏感字段以掩码展示，明文永不下发 |
| AWS 区域校正 | region 误填成可用区（如 ap-southeast-1a）时实时提示并支持一键修正 |
| Cloudflare 配置 | 在线编辑 Cloudflare 凭据。支持 API Token（权限只需 Zone→DNS→Edit 与 Zone→Zone→Read）和 Global API Key 两种方式，两者并存时以 Token 为准 |
| 网络配置 | 在线编辑本机地址、URL 名称、启动模式 |
| 配置热加载 | 保存后即时热加载生效，也可手动刷新内存配置，均无需重启客户端 |

---

## 主题

Web 界面支持 8 种主题，每种均包含亮色和暗色两种模式：

| 主题 | 风格 |
|------|------|
| 经典 | 默认蓝色，简洁商务 |
| 樱花 | 粉色系，二次元 / ACG 风 |
| 赛博 | 霓虹青蓝，科技感 |
| 水墨 | 金色古朴，国风 |
| 极光 | 青绿紫渐变，北极光 |
| 星夜 | 深紫色调，星空 |
| 海渊 | 深海蓝绿，荧光点缀 |
| 日落 | 暖橘珊瑚色，温馨 |

在顶部栏的主题选择器中切换主题，点击明暗模式按钮切换亮色/暗色。

---

## 云实例一键 SSH

所有云平台（OCI / AWS EC2 / AWS Lightsail / GCP / Azure / DO / SolusVM / VirtFusion）的实例卡片上均提供「SSH」快捷按钮，点击后直接跳转到终端并连接该实例，无需手动填写连接信息。每张卡片还提供 IP 一键复制按钮。

---

## 各云体检面板

汇总展示所有云平台的实例总量、运行/停止数、DNS 信息等关键指标，一屏纵览多云资产健康度。

---

## 工作台体验

- **按优先级排序** — 云平台标签页按使用频率排列（OCI、AWS、Azure、GCP、DO、Cloudflare、SolusVM、VirtFusion）
- **表单化配置上传** — 配置上传改为表单输入，无需手写格式
- **按钮分组重排** — 实例操作按钮按使用频率分组排列

---

## 在线客服

页面右下角的悬浮按钮打开内置聊天窗口，可直接与客服沟通，支持发送图片消息。图标支持**自动隐藏**和**自由拖动**，不遮挡操作区域。
